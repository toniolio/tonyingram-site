# tonyingram-site

Source for the public `tonyingram.com` website.

This repo contains a small Astro-based static site that recreates the current public pages and keeps site assets in-repo.

Operational notes for local setup, rebuilds, and deployment live in `docs/maintenance.md`.
